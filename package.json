{
  "private": true,
  "scripts": {
    "build": "pnpm --filter @sorrycc/* run build",
    "dev": "pnpm --filter @sorrycc/* run dev"
  },
  "devDependencies": {
    "core-js": "^3.37.0",
    "node-libs-browser-okam": "^2.2.4"
  }
}
